$(document).ready(function() {

    var minNumber = 1;
    var maxNumber = 100;
    var targetNumber = Math.floor(Math.random() * (maxNumber - minNumber + 1) + minNumber);

    $("#targetNumber").append(" " + targetNumber);
    
    var wins=0;
    var lives=3;

    var counter=0;

    var ghostNumbers = [20, 9, 55, 1];










});   